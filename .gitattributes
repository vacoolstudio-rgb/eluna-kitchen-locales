# Пакеты проверяются по sha256 из manifest.json. Любая правка байтов — в том
# числе замена переводов строк — ломает проверку в приложении. Поэтому всё
# считается двоичным и никогда не переписывается git'ом.
*.json -text
*.gz -text binary
